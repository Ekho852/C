#include <stdio.h>

int main()

{   //assign variables 
    int num;

    // set for loop
    for (num = 1; num <= 49; num+=9)
    {
        // print for loop output
        printf("%d, " , num);
    }
    return 0;
}